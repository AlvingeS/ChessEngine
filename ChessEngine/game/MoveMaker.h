#pragma once

namespace game {
    class MoveMaker {
        public:
            // Constructor
            MoveMaker();

            // Public member functions
            void makeMove();
            void unmakeMove();

        private:
    };
}